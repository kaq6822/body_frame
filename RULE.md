Flutter 앱을 구현하고 테스트할 때 다음 원칙을 따른다.

[앱 구현 시]

1. 주요 화면, 버튼, 입력란, 상태 표시, 결과 영역에
   변경되지 않는 고유 식별자를 부여한다.

2. 외부 자동화 도구가 요소를 찾을 수 있도록
   주요 요소에 Semantics.identifier를 지정하고,
   label, role, enabled, selected, value 등의 의미와 상태도 제공한다.

3. Flutter 내부 테스트에서도 동일한 요소를 찾을 수 있도록
   주요 요소에 안정적인 ValueKey를 함께 지정한다.

4. 식별자는 화면 문구, 색상, 위치, 순서에 의존하지 않도록 한다.
   예:
   - screen.profile
   - profile.edit.button
   - profile.save.status
   - profile.avatar.image

5. GestureDetector, CustomPaint, 이미지 버튼 등
   외부에서 의미를 알기 어려운 커스텀 UI에는 반드시
   적절한 Semantics 역할, 이름, 상태, 동작을 제공한다.

6. 비동기 작업은 단순 로딩 화면만 표시하지 말고
   대기, 진행 중, 성공, 실패 상태를 명확하게 구분하여
   UI 계층과 화면에서 확인할 수 있도록 구현한다.

7. AI가 결과를 확인해야 하는 주요 영역에는 별도의 식별자를 부여한다.
   전체 화면뿐 아니라 해당 영역만 캡처하고 검사할 수 있어야 한다.

8. 테스트할 기능은 실제 장치 상태나 운영 데이터에만 의존하지 않도록 한다.
   필요한 경우 테스트 데이터, 고정된 fixture, 가짜 응답 또는
   테스트용 계정을 주입할 수 있는 구조로 구현한다.

9. QA 또는 staging 환경을 운영 환경과 분리한다.
   QA 환경에서는 앱 상태 초기화, 테스트 사용자 로그인,
   특정 화면 진입, 테스트 데이터 선택이 가능해야 한다.

10. 같은 테스트를 반복했을 때 결과가 달라지지 않도록
    랜덤 데이터, 현재 시간, 네트워크 지연, 팝업,
    애니메이션, 캐시 상태 등을 통제할 수 있게 한다.

11. 기능의 시작, 진행, 성공, 실패와 주요 렌더링 결과를
    구조화된 로그로 남긴다.
    단, 개인정보와 인증 정보는 로그에 포함하지 않는다.

12. 시각적 정상 기준이 필요한 화면은 요구사항으로 명시한다.
    잘림 허용 여부, 비율 유지 여부, 오버플로 허용 여부,
    최소 여백, 표시해야 하는 콘텐츠 등을 문서화한다.

13. AI 또는 테스트 자동화를 위해 앱 내부에서 LLM을 실행하지 않는다.
    앱은 식별 가능한 UI, 재현 가능한 상태와 검사 가능한 결과만 제공하고,
    AI Agent가 외부에서 앱을 조작하고 판단하도록 구성한다.


[AI Agent 테스트 수행 시]

14. 화면 좌표를 추정해서 클릭하는 방식은 최후의 수단으로 사용한다.
    Semantics identifier, accessibility identifier, resource-id,
    텍스트와 역할 등 UI 계층 정보를 우선 사용한다.

15. 테스트 시작 전에 앱 상태, 로그인 상태, 테스트 데이터,
    권한과 팝업 상태를 초기화하거나 확인한다.

16. 고정된 시간만 기다리지 않는다.
    작업 완료 상태, 로딩 종료, 화면 전환 또는 결과 요소 표시를
    확인한 뒤 다음 단계로 진행한다.

17. 기능 성공 여부와 시각적 표시 결과를 별도로 검증한다.
    기능이 성공했더라도 최종 화면의 잘림, 왜곡, 겹침,
    오버플로, 비정상 여백과 가독성 문제를 직접 확인한다.

18. 시각적 검사가 필요한 경우 전체 화면과 대상 영역을 각각 캡처한다.
    요구사항이나 기준 화면을 바탕으로 정상 여부를 판단한다.

19. 예상 결과가 명확하지 않으면 임의로 정상 판정하지 않는다.
    코드, 요구사항, 디자인 문서 또는 테스트 명세를 확인한다.

20. 좌표 방식으로 조작한 경우 그 사실과 불확실성을 결과에 기록하고,
    가능하면 UI 식별자를 사용하는 방식으로 다시 검증한다.

21. 테스트 결과에는 다음 내용을 포함한다.
    - 수행한 환경과 앱 버전
    - 수행 단계
    - 기능 테스트 결과
    - 시각적 테스트 결과
    - 기대 결과와 실제 결과
    - 재현 단계
    - 관련 스크린샷과 로그
