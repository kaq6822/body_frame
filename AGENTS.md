# Body Frame 개발 지침

이 문서는 코드와 설정만으로는 드러나지 않는 제약만 기록한다. 모델 필드, 라우트,
의존성 버전, 디렉터리 구조처럼 코드에서 바로 확인할 수 있는 정보는 추가하지 않는다.
남은 구현과 배포 준비는 `ROADMAP.md`에서만 관리한다.

## 제품 경계

- 개인이 자기 체형 변화를 기록하는 Android/iOS 오프라인 앱이다. 별도 결정 없이
  로그인, 서버, 클라우드 동기화 또는 핵심 기능의 네트워크 의존성을 추가하지 않는다.
- 사용 주체는 한 사람이다. 회원/프로필/다중 사용자 개념을 다시 도입하지 않는다.
  본인 외의 사람을 찍은 기록은 `PhotoRecord.label` 자유 문자열로만 구분한다.
- 앱 잠금, PIN, 생체 인증, 암호화 백업, 워터마크는 의도적으로 제거한 기능이다.
  요구가 새로 생기기 전에는 다시 넣지 않는다.

## 데이터 보관과 이전

- 데이터 이전은 OS의 **기기 간 전송(D2D)** 에만 위임한다. 앱 안에 백업/복원
  기능을 만들지 않는다.
- 클라우드 자동 백업은 사용하지 않는다. Android Auto Backup은 앱당 25MB
  한도라 사진을 담을 수 없고, 일부만 올라간 백업은 복원 결과를 신뢰할 수 없게
  만든다. `data_extraction_rules.xml`의 `cloud-backup`은 전부 제외 상태를
  유지하고 `device-transfer`만 포함한다.
- Android `allowBackup`을 false로 되돌리지 않는다. 제조사에 따라 D2D 전송까지
  막혀 기기 교체 시 기록이 사라진다. 무엇을 넘길지는 `dataExtractionRules`에서만
  결정한다.
- iOS는 iCloud 백업과 Quick Start 전송을 나누는 스위치가 없다. 앱 컨테이너에
  `isExcludedFromBackup`이나 `FileProtectionType.complete`를 설정하면 기기
  교체 시 기록이 함께 사라지므로 기본값을 그대로 둔다.

## 데이터와 사진 불변식

- 사진 바이트는 DB에 넣지 않는다. 앱 전용 저장소의 파일 경로와 메타데이터만
  저장한다. `body_photos.file_path`에는 `photos/{yyyyMM}/...` 상대경로만 넣는다.
  앱 컨테이너 절대경로는 기기 이전으로 바뀌므로 저장하지 않는다.
- 체형 사진 원본을 자동 크롭, 보정, 변형 또는 덮어쓰지 않는다. 표시와 파생
  이미지에서는 원본 비율과 방향을 보존하고, 결과물은 별도 파일로 생성한다.
- 삭제에는 명시적 확인이 필요하다. DB 행과 파일을 함께 정리하며, 삭제 경로에서
  리포지토리와 저장소 정리를 우회하지 않는다. DB cascade는 사진 파일을 지우지
  않는다.
- 파일 쓰기는 staging에 먼저 기록하고 rename으로 확정한다. 목적지에 반쯤 쓰인
  파일이 남으면 안 된다.
- DB 스키마를 바꾸면 `schemaVersion`과 누적 마이그레이션을 함께 추가한다.

## 구현과 테스트 계약

- 앱 코드에서는 Riverpod provider가 노출하는 리포지토리·서비스 인터페이스를
  사용한다. 파일·카메라·공유 등 플랫폼 I/O는 테스트에서 대체 가능하게 유지한다.
- 주요 화면, 입력, 조작, 상태와 결과 영역에는 문구나 위치에 의존하지 않는
  `Semantics.identifier`와 `ValueKey`를 함께 둔다. 기존 `screen.<feature>.<name>` /
  `<feature>.<target>.<kind>` 규칙을 이어 쓴다.
- 커스텀 제스처와 페인터에는 역할, 상태, 값과 동작을 Semantics로 제공한다.
  비동기 UI는 진행, 성공·빈 상태, 실패를 구분하고 실패 시 재시도를 제공한다.
- 테스트는 현재 시간, 랜덤 값이나 실제 기기 데이터에 의존하지 않게 하고, 플랫폼
  I/O는 대체 가능한 경계를 통해 결정론적으로 검증한다.
- 사진 UI를 변경하면 여러 종횡비에서 잘림·왜곡·오버플로가 없는지 확인한다.
  비교 미리보기와 생성 이미지의 구도도 일치해야 한다.

## 작업 및 검증

- 변경 범위의 테스트를 추가하고 `flutter analyze`와 `flutter test`를 실행한다.
  카메라, 갤러리, 저장·공유 플러그인 변경은 Android/iOS에서도 확인한다.
- 기존 전체 포맷 차이를 정리하려는 목적이 아니라면 수정한 Dart 파일만 포맷한다.
- 플랫폼 프로젝트를 재생성하지 않는다. 불가피하게 수정할 때는 Android의 권한과
  백업 규칙, `FlutterFragmentActivity`, iOS의 권한 설명을 보존한다.
