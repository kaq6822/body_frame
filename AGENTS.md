# Body Frame 개발 지침

이 문서는 코드와 설정만으로는 드러나지 않는 제약만 기록한다. 모델 필드, 라우트,
의존성 버전, 디렉터리 구조처럼 코드에서 바로 확인할 수 있는 정보는 추가하지 않는다.
남은 제품 작업은 `ROADMAP.md`에서만 관리한다.

## 제품 경계

- Android/iOS용 오프라인 로컬 앱이다. 별도 결정 없이 로그인, 서버, 클라우드
  동기화 또는 핵심 기능의 네트워크 의존성을 추가하지 않는다.
- 회원 정보와 체형 사진은 민감정보다. 사용자가 명시적으로 내보내거나 공유한
  경우에만 앱 전용 저장소 밖으로 전달한다.
- 백업 ZIP은 현재 암호화되지 않는다. 암호화를 구현하기 전까지 민감정보 포함
  경고와 안전한 보관 안내를 유지한다.

## 데이터와 사진 불변식

- 사진 바이트는 DB에 넣지 않는다. 앱 전용 저장소의 파일 경로와 메타데이터만
  저장한다.
- 체형 사진 원본을 자동 크롭, 보정, 변형 또는 덮어쓰지 않는다. 표시와 파생
  이미지에서는 원본 비율과 방향을 보존하고, 결과물은 별도 파일로 생성한다.
- 삭제, 전체 복원, 데이터 교체에는 명시적 확인이 필요하다. DB 행과 파일을 함께
  정리하며, 검증에 실패한 백업이 기존 데이터를 변경해서는 안 된다.
- DB 스키마를 바꾸면 `schemaVersion`과 누적 마이그레이션을 함께 추가한다. 백업
  형식을 바꿀 때도 형식 버전과 이전 데이터 호환 테스트를 함께 갱신한다.
- DB cascade는 사진 파일을 삭제하지 않는다. 삭제 경로에서 리포지토리와 저장소
  정리를 우회하지 않는다.
- 이름, 연락처, 메모, 인증정보, 사진 경로와 이미지 내용은 로그에 남기지 않는다.
  로그 문맥은 비식별 ID, 개수와 상태처럼 필요한 최소 정보만 사용한다.

## 구현과 테스트 계약

- 앱 코드에서는 Riverpod provider가 노출하는 리포지토리·서비스 인터페이스를
  사용한다. 파일·카메라·공유 등 플랫폼 I/O는 테스트에서 대체 가능하게 유지한다.
- 주요 화면, 입력, 조작, 상태와 결과 영역에는 문구나 위치에 의존하지 않는
  `Semantics.identifier`와 `ValueKey`를 함께 둔다. 기존 `screen.<feature>.<name>` /
  `<feature>.<target>.<kind>` 규칙을 이어 쓴다.
- 커스텀 제스처와 페인터에는 역할, 상태, 값과 동작을 Semantics로 제공한다.
  비동기 UI는 진행, 성공·빈 상태, 실패를 구분하고 실패 시 재시도를 제공한다.
- 테스트는 현재 시간, 랜덤 값, 실제 기기 데이터에 의존하지 않게 한다. DB 테스트는
  기존 FFI 초기화 패턴을, 플랫폼 기능은 provider fake와 설정 mock을 재사용한다.
- 사진 UI를 변경하면 여러 종횡비에서 잘림·왜곡·오버플로가 없는지 확인한다.
  비교 미리보기와 생성 이미지의 구도도 일치해야 한다.

## 작업 및 검증

- 초기 설정은 `flutter pub get`으로 수행한다. `pod install`을 직접 실행해야 한다면
  반드시 그 뒤에 실행한다.
- 변경 범위의 테스트를 추가하고 `flutter analyze`와 `flutter test`를 실행한다.
  카메라, 갤러리, 생체 인증, 저장·공유 플러그인 변경은 Android/iOS에서도 확인한다.
- 기존 전체 포맷 차이를 정리하려는 목적이 아니라면 수정한 Dart 파일만 포맷한다.
- 플랫폼 프로젝트를 재생성하지 않는다. 불가피하게 수정할 때는 Android의 권한,
  백업 차단과 `FlutterFragmentActivity`, iOS의 권한 설명을 보존한다.
- Android release 빌드는 현재 debug key로 서명된다. 정식 서명을 구성하기 전에는
  배포 가능한 산출물로 취급하지 않는다.
